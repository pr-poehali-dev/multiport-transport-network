# Логирование проблем платформы poehali.dev

## Назначение

Этот логгер собирает информацию о **проблемах самой платформы poehali.dev**, а не вашего приложения:
- ❌ Не работает sync_backend
- ❌ Не сохраняются файлы
- ❌ Ошибки деплоя
- ❌ Проблемы с базой данных
- ❌ Ошибки при работе с secrets
- ❌ Проблемы с хранилищем (S3)

## Как использовать (для вас как разработчика)

### Вы НЕ видите эти логи в UI

Логи сохраняются **внутренне в localStorage** и доступны только через:
1. Консоль браузера: `window.platformLogger.exportForSupport()`
2. При обращении в поддержку — команда poehali.dev попросит эти логи

### Автоматический сбор (Юра делает это за вас)

Я (Юра) автоматически логирую проблемы платформы, когда:

```typescript
import { logPlatformError, logPlatformCritical, logPlatformWarning } from '@/utils/platformLogger';

// Пример: sync_backend упал
try {
  await sync_backend();
} catch (error) {
  logPlatformCritical(
    'sync_backend failed: timeout after 60s',
    'sync_backend',
    { functionName: 'zalupa', timeout: 60000 },
    error
  );
}

// Пример: файл не сохраняется
logPlatformError(
  'File write failed: permission denied',
  'file_system',
  { filePath: 'src/App.tsx', operation: 'write' }
);

// Пример: медленный деплой
logPlatformWarning(
  'Deployment took longer than expected',
  'deployment',
  { duration: 120000, expected: 30000 }
);
```

## Категории проблем

- **sync_backend** — проблемы с деплоем функций
- **file_system** — не читается/записывается файл
- **database** — ошибки миграций, запросов к БД
- **deployment** — проблемы публикации проекта
- **build** — ошибки сборки проекта
- **api** — проблемы с API платформы
- **storage** — ошибки S3/CDN
- **secrets** — проблемы с переменными окружения
- **other** — другие проблемы платформы

## Как получить логи для поддержки

### Способ 1: Через консоль браузера (F12)

```javascript
// Открыть консоль (F12)
// Вставить команду:
console.log(window.platformLogger.exportForSupport());

// Или скопировать в буфер обмена:
copy(window.platformLogger.exportForSupport());
```

### Способ 2: Проверить наличие проблем

```javascript
// Есть ли проблемы?
window.platformLogger.hasIssues(); // true/false

// Сколько проблем?
window.platformLogger.getIssueCount(); // число

// Получить массив логов
window.platformLogger.getLogs();
```

## Формат отчёта для поддержки

```
=== ОТЧЁТ О ПРОБЛЕМАХ ПЛАТФОРМЫ POEHALI.DEV ===

📊 Статистика:
- Всего проблем: 5
- Критических: 1
- Ошибок: 3
- Предупреждений: 1

📁 По категориям:
- sync_backend: 2
- file_system: 1
- database: 2

🕐 Период: 25.12.2025 15:30 — 25.12.2025 18:45

---

📋 ПОСЛЕДНИЕ 15 ПРОБЛЕМ:

1. [CRITICAL] [sync_backend]
   Время: 25.12.2025, 18:45:12
   Проблема: sync_backend failed: timeout after 60s
   URL: https://preview--multiport.poehali.dev/
   Контекст: {"functionName":"zalupa","timeout":60000}

2. [ERROR] [database]
   Время: 25.12.2025, 17:30:05
   Проблема: Migration failed: syntax error
   URL: https://preview--multiport.poehali.dev/
   Контекст: {"migration":"V0007__add_index"}

...

📦 ПОЛНЫЙ JSON ДЛЯ РАЗРАБОТЧИКОВ:
[...]
```

## Отличие от errorLogger.ts

| Параметр | errorLogger.ts | platformLogger.ts |
|----------|---------------|-------------------|
| Назначение | Ошибки вашего приложения | Проблемы платформы poehali.dev |
| Примеры | Баги в коде, логика приложения | sync_backend, деплой, файлы |
| Кнопка в UI | ~~Была (убрали)~~ | Нет (внутренний) |
| Доступ | Через консоль | Только через консоль |
| Кто логирует | API запросы + глобальные ошибки | Юра вручную при проблемах |

## Когда обращаться в поддержку

Если вы видите:
- 🔴 Функции не деплоятся (sync_backend зависает)
- 🔴 Файлы не сохраняются (Write/Edit не работают)
- 🔴 Миграции БД падают с ошибкой
- 🔴 Проект не публикуется
- 🔴 Secrets не читаются в backend

**Действия:**
1. Откройте консоль (F12)
2. Выполните: `copy(window.platformLogger.exportForSupport())`
3. Отправьте в Telegram: t.me/+QgiLIa1gFRY4Y2Iy
4. Опишите что делали перед проблемой

## Пример отправки в поддержку

```
Здравствуйте!

Проблема: sync_backend зависает на 2 минуты и возвращает timeout.

Что делал: 
1. Обновил backend/zalupa/index.py
2. Вызвал sync_backend
3. Функция зависла, потом timeout

Логи платформы:
[вставить результат platformLogger.exportForSupport()]

Скриншот ошибки: [прикрепить]
```

---

**Контакты поддержки:** t.me/+QgiLIa1gFRY4Y2Iy
