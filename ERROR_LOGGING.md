# Система логирования ошибок для poehali.dev

## Что это?

Автоматическая система сбора ошибок, которая помогает:
- **Вам** — отслеживать проблемы в приложении
- **Команде poehali.dev** — диагностировать баги платформы

## Как это работает?

### 1. Автоматический сбор
Система автоматически ловит:
- ✅ Ошибки JavaScript (window.error)
- ✅ Необработанные Promise reject (unhandledrejection)
- ✅ Ошибки API запросов (fetch fails)
- ✅ HTTP ошибки от backend (4xx, 5xx)

### 2. Хранение логов
- Логи сохраняются в **localStorage** браузера
- Хранится последние **100 записей**
- Доступны даже после перезагрузки страницы

### 3. Просмотр логов
В правом верхнем углу каждой страницы есть кнопка 🐛 (Bug):
- **Без ошибок** — только иконка
- **Есть ошибки** — красный badge с количеством

## Как использовать?

### Для отправки в поддержку

1. Нажмите кнопку 🐛 в правом верхнем углу
2. Нажмите **"Скопировать для поддержки"**
3. Отправьте скопированный текст в поддержку poehali.dev (t.me/+QgiLIa1gFRY4Y2Iy)

### Формат логов

Каждая запись содержит:
```json
{
  "timestamp": "2025-12-25T18:30:00.000Z",
  "severity": "error",
  "message": "API Error: Failed to fetch",
  "url": "https://preview--multiport.poehali.dev/drivers",
  "userAgent": "Mozilla/5.0...",
  "stack": "Error: Failed to fetch\n    at...",
  "context": {
    "url": "https://functions.poehali.dev/...",
    "status": 500,
    "method": "POST"
  }
}
```

### Уровни серьёзности

- **error** (красный) — критические ошибки
- **warning** (оранжевый) — предупреждения
- **info** (синий) — информационные сообщения

## Добавление логов вручную

В коде можно логировать свои события:

```typescript
import { logError, logWarning, logInfo } from '@/utils/errorLogger';

// Ошибка
try {
  // ... код
} catch (error) {
  logError('Не удалось сохранить водителя', error, {
    driverId: 123,
    action: 'save'
  });
}

// Предупреждение
logWarning('Медленный запрос к API', {
  url: '/api/drivers',
  duration: 5000
});

// Информация
logInfo('Пользователь экспортировал данные', {
  format: 'PDF',
  count: 50
});
```

## Очистка логов

Нажмите **"Очистить логи"** в окне просмотра — все записи будут удалены из localStorage.

## Что НЕ логируется?

- ❌ Пароли и секреты
- ❌ Персональные данные пользователей
- ❌ Содержимое форм

Логируются только:
- ✅ Технические ошибки
- ✅ URL запросов
- ✅ HTTP статус коды
- ✅ Stack traces

## Пример отправки в поддержку

```
Здравствуйте! У меня проблема с сохранением водителей.
Вот логи ошибок:

=== POEHALI.DEV ERROR LOGS ===
Всего записей: 3
Ошибок: 2
Предупреждений: 1

1. [ERROR] 2025-12-25T18:30:15.000Z
   API Error: Водитель Иванов Иван с телефоном +7 (999) 123-45-67 уже существует
   URL: https://preview--multiport.poehali.dev/drivers/add
   Stack: Error: API Error: Водитель Иванов Иван...

...

(полный JSON лог ниже)
```

## Контакты поддержки

- Telegram: https://t.me/+QgiLIa1gFRY4Y2Iy
- При обращении всегда прикладывайте логи из кнопки 🐛
